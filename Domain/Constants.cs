namespace Domain
{
    // The class to hold domain constants.
    public static class Constants
    {
        // Holds the url path to get the token.
        public const string TokenPath = "/Token";
        
        // Holds the url path to make the search.
        public const string SearchPath = "/api/Ticket/Search";
    }
}